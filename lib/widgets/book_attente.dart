import 'package:flutter/cupertino.dart';

class BookEtat extends StatelessWidget {
  const BookEtat({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
